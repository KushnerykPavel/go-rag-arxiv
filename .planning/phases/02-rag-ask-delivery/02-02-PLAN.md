---
phase: 02-rag-ask-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - internal/client/groq/config.go
  - internal/client/groq/client.go
  - internal/rag/ask_pipeline.go
  - internal/server/grpc/arxiv.go
  - internal/app/app.go
  - internal/app/config.go
autonomous: true
requirements:
  - RAG-02
  - RAG-03
  - APC-03
user_setup:
  - service: groq
    why: "Live Ask generation backend"
    env_vars:
      - name: PRODUCER_GROQ_API_KEY
        source: "Groq Console -> API Keys"
must_haves:
  truths:
    - "Ask performs paper retrieval before generation (RAG-02)."
    - "Ask response includes deterministic citations from retrieved papers (RAG-03)."
    - "Ask maps invalid and downstream failures to stable gRPC status codes (APC-03)."
  artifacts:
    - "internal/rag/ask_pipeline.go orchestrates validate -> retrieve -> generate -> citations."
    - "internal/client/groq/client.go provides generation client used by Ask path."
    - "internal/server/grpc/arxiv.go exposes Ask handler calling the pipeline and mapping errors."
    - "internal/app/config.go requires GROQ key once Ask runtime path is active."
  key_links:
    - "internal/server/grpc/arxiv.go -> internal/rag/ask_pipeline.go via injected Ask service interface"
    - "internal/rag/ask_pipeline.go -> internal/client/arxiv fetcher before generation call"
    - "internal/rag/ask_pipeline.go -> internal/client/groq/client.go for answer generation"
---

<objective>
Implement the Ask runtime path with retrieval-first orchestration, citation assembly, and stable transport error mapping.

Purpose: Deliver the core behavior users expect from RAG Ask while preserving deterministic server behavior.
Output: New Groq client, Ask pipeline, gRPC Ask handler wiring, and active runtime config for Groq.
</objective>

<execution_context>
@/Users/pavelkushneryk/Documents/vsprojects/go-rag-arxiv/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pavelkushneryk/Documents/vsprojects/go-rag-arxiv/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rag-ask-delivery/02-RESEARCH.md
@.planning/phases/01-contract-baseline/01-02-SUMMARY.md
@internal/client/arxiv/client.go
@internal/client/arxiv/paper.go
@internal/server/grpc/arxiv.go
@internal/app/app.go
@internal/app/config.go

<interfaces>
From internal/client/arxiv/client.go:
```go
func (c *Client) FetchPapersWithQuery(ctx context.Context, query string, params FetchParams) ([]Paper, error)
```

From internal/client/arxiv/paper.go:
```go
type Paper struct {
	ArxivID  string
	Title    string
	Abstract string
	PDFURL   string
}
```

From internal/app/config.go:
```go
type Config struct {
	GroqAPIKey string `envconfig:"GROQ_API_KEY"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Ask pipeline contracts before wiring (RAG-02/RAG-03)</name>
  <read_first>
    - .planning/phases/02-rag-ask-delivery/02-RESEARCH.md
    - internal/server/grpc/arxiv.go
    - internal/client/arxiv/paper.go
    - internal/client/arxiv/client.go
  </read_first>
  <files>internal/rag/ask_pipeline.go</files>
  <action>Create `internal/rag/ask_pipeline.go` with explicit contracts first: request/result structs, citation struct (`id/title/url`), small interfaces for retriever and generator, and a service constructor; define exported sentinel errors for invalid input, empty retrieval, upstream unavailable, and rate-limit cases to support APC-03 mapping in transport layer.</action>
  <acceptance_criteria>
    - `rg "type AskRequest|type AskResult|type Citation|var ErrAsk" internal/rag/ask_pipeline.go` returns matches.
    - `go test ./internal/rag -count=1` succeeds (compile-level if no tests yet).
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/rag -count=1</automated>
  </verify>
  <done>Ask contracts are explicit and usable by handler/client wiring without additional codebase exploration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Groq client and retrieval-first Ask orchestration (RAG-02/RAG-03)</name>
  <read_first>
    - internal/rag/ask_pipeline.go
    - internal/client/arxiv/client.go
    - internal/client/arxiv/paper.go
    - .planning/phases/02-rag-ask-delivery/02-RESEARCH.md
  </read_first>
  <files>internal/client/groq/config.go, internal/client/groq/client.go, internal/rag/ask_pipeline.go</files>
  <action>Create `internal/client/groq/config.go` and `internal/client/groq/client.go` as a thin HTTP chat-completions client using `https://api.groq.com/openai/v1/chat/completions`; in `internal/rag/ask_pipeline.go` implement flow in exact order: validate query and limit, call retrieval first (`FetchPapersWithQuery`), fail on empty retrieval, build bounded context from retrieved papers, call generator, require non-empty answer, then build citations deterministically from retrieved papers (`ArxivID`, `Title`, `PDFURL`) per RAG-02/RAG-03.</action>
  <acceptance_criteria>
    - `rg "FetchPapersWithQuery\\(" internal/rag/ask_pipeline.go` returns one or more matches.
    - `rg "type Citation struct|Id|Title|URL|PDFURL" internal/rag/ask_pipeline.go` returns matches showing deterministic citation mapping.
    - `rg "api.groq.com/openai/v1/chat/completions|Authorization" internal/client/groq/client.go` returns matches.
    - `go test ./internal/client/groq ./internal/rag -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/client/groq ./internal/rag -count=1</automated>
  </verify>
  <done>Ask pipeline enforces retrieval-before-generation and returns structured citations sourced from retrieval results.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Ask into gRPC handler and runtime config with stable error mapping (APC-03)</name>
  <read_first>
    - internal/server/grpc/arxiv.go
    - internal/app/app.go
    - internal/app/config.go
    - internal/rag/ask_pipeline.go
    - internal/gen/arxiv/v1/arxiv.pb.go
  </read_first>
  <files>internal/server/grpc/arxiv.go, internal/app/app.go, internal/app/config.go</files>
  <action>Update `internal/server/grpc/arxiv.go` to implement `Ask(ctx, req)` using the new pipeline service and map pipeline/downstream errors to stable gRPC codes (`InvalidArgument`, `NotFound`, `DeadlineExceeded`, `Unavailable`, `ResourceExhausted`, fallback `Internal`) per APC-03; update `internal/app/app.go` to instantiate Groq client + Ask service and inject it into `NewArxivHandler`; update `internal/app/config.go` validation so `GROQ_API_KEY` is required now that Ask runtime behavior is active (consistent with Phase 1 decision history).</action>
  <acceptance_criteria>
    - `rg "func \\(h \\*ArxivHandler\\) Ask\\(" internal/server/grpc/arxiv.go` returns one match.
    - `rg "codes\\.InvalidArgument|codes\\.NotFound|codes\\.DeadlineExceeded|codes\\.Unavailable|codes\\.ResourceExhausted|codes\\.Internal" internal/server/grpc/arxiv.go` returns matches.
    - `rg "missing required runtime config: GROQ_API_KEY" internal/app/config.go` returns one match.
    - `go test ./internal/app ./internal/server/grpc -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app ./internal/server/grpc -count=1</automated>
  </verify>
  <done>Ask runtime path is wired end-to-end, and transport returns deterministic status codes for known failure classes.</done>
</task>

</tasks>

<verification>
- Run focused package tests: `go test ./internal/client/groq ./internal/rag ./internal/server/grpc ./internal/app -count=1`
- Run full compile/test baseline: `go test ./... -count=1`
</verification>

<success_criteria>
- RAG-02 satisfied: generation is gated by successful non-empty retrieval.
- RAG-03 satisfied: Ask response returns structured citations from retrieved papers.
- APC-03 implemented at handler boundary with explicit stable code mapping.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rag-ask-delivery/02-rag-ask-delivery-02-SUMMARY.md`
</output>
