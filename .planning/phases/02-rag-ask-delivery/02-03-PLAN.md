---
phase: 02-rag-ask-delivery
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - internal/server/grpc/arxiv_ask_test.go
  - internal/rag/ask_pipeline_test.go
  - internal/client/groq/client_test.go
  - internal/rag/ask_pipeline.go
  - internal/client/groq/client.go
autonomous: true
requirements:
  - RAG-01
  - RAG-02
  - RAG-03
  - APC-03
must_haves:
  truths:
    - "Ask returns non-empty answer and citations for valid requests (RAG-01/RAG-03)."
    - "Ask test coverage proves retrieval occurs before generation (RAG-02)."
    - "Ask failure paths return deterministic status codes across reruns (APC-03)."
  artifacts:
    - "internal/server/grpc/arxiv_ask_test.go covers success and status-code mapping matrix."
    - "internal/rag/ask_pipeline_test.go covers retrieval-first behavior and citation construction."
    - "internal/client/groq/client_test.go covers deterministic downstream HTTP/timeout/rate-limit handling."
  key_links:
    - "grpc Ask tests -> handler error mapper -> gRPC `codes.*` assertions"
    - "pipeline tests -> retrieval mock call order before generator mock invocation"
    - "groq client tests -> HTTP failure types -> pipeline sentinel errors"
---

<objective>
Add deterministic automated verification for Ask behavior, grounding guarantees, and stable error mapping.

Purpose: Close Phase 2 Nyquist gaps with repeatable tests that prevent regressions in the new Ask path.
Output: Ask transport tests, pipeline orchestration tests, and Groq client failure-mode tests.
</objective>

<execution_context>
@/Users/pavelkushneryk/Documents/vsprojects/go-rag-arxiv/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pavelkushneryk/Documents/vsprojects/go-rag-arxiv/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rag-ask-delivery/02-VALIDATION.md
@.planning/phases/02-rag-ask-delivery/02-RESEARCH.md
@internal/server/grpc/arxiv.go
@internal/rag/ask_pipeline.go
@internal/client/groq/client.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Ask transport tests for success and status mapping (RAG-01/APC-03)</name>
  <read_first>
    - .planning/phases/02-rag-ask-delivery/02-VALIDATION.md
    - internal/server/grpc/arxiv.go
    - internal/server/grpc/arxiv_ask_test.go
    - internal/gen/arxiv/v1/arxiv.pb.go
  </read_first>
  <files>internal/server/grpc/arxiv_ask_test.go</files>
  <behavior>
    - Test 1: valid Ask request returns non-empty `answer` with at least one citation item.
    - Test 2: empty query returns `codes.InvalidArgument`.
    - Test 3: retrieval empty/error and generation timeout/rate-limit map to documented APC-03 codes.
  </behavior>
  <action>Implement table-driven tests in `internal/server/grpc/arxiv_ask_test.go` with deterministic fakes for retrieval/generation/pipeline outputs; assert exact gRPC status codes and message stability for each scenario: invalid input, no results, deadline exceeded, unavailable downstream, resource exhausted, and internal fallback.</action>
  <acceptance_criteria>
    - `rg "TestAskSuccess|TestAskStatusMapping|codes\\.InvalidArgument|codes\\.ResourceExhausted" internal/server/grpc/arxiv_ask_test.go` returns matches.
    - `go test ./internal/server/grpc -run 'TestAskSuccess|TestAskStatusMapping' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/server/grpc -run 'TestAskSuccess|TestAskStatusMapping' -count=1</automated>
  </verify>
  <done>Transport-level Ask contract and APC-03 mapping are enforced by deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add retrieval-first pipeline and citation tests (RAG-02/RAG-03)</name>
  <read_first>
    - internal/rag/ask_pipeline.go
    - internal/client/arxiv/paper.go
    - .planning/phases/02-rag-ask-delivery/02-RESEARCH.md
  </read_first>
  <files>internal/rag/ask_pipeline_test.go, internal/rag/ask_pipeline.go</files>
  <behavior>
    - Test 1: generator is never called when retrieval fails or returns zero papers.
    - Test 2: successful pipeline returns citations mapped from retrieved papers (`id/title/url`).
    - Test 3: answer must be non-empty for successful Ask result.
  </behavior>
  <action>Create `internal/rag/ask_pipeline_test.go` with call-order assertions using fakes/spies to prove retrieval executes before generation and generation is skipped on retrieval failure/empty result; assert citation values are copied from retrieved `arxiv.Paper` fields; adjust `internal/rag/ask_pipeline.go` only if needed to make behavior deterministic and testable.</action>
  <acceptance_criteria>
    - `rg "TestAskPipelineRetrievalFirst|TestAskPipelineCitations|TestAskPipelineRejectsEmptyAnswer" internal/rag/ask_pipeline_test.go` returns matches.
    - `go test ./internal/rag -run 'TestAskPipeline' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/rag -run 'TestAskPipeline' -count=1</automated>
  </verify>
  <done>Pipeline behavior for grounding and citation traceability is proven by deterministic unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Groq client deterministic failure-mode tests and phase gate (APC-03)</name>
  <read_first>
    - internal/client/groq/client.go
    - .planning/phases/02-rag-ask-delivery/02-VALIDATION.md
    - .planning/phases/02-rag-ask-delivery/02-RESEARCH.md
  </read_first>
  <files>internal/client/groq/client_test.go, internal/client/groq/client.go</files>
  <behavior>
    - Test 1: 200 completion response returns non-empty content.
    - Test 2: timeout/context cancellation is classified for deadline mapping.
    - Test 3: 429 and 5xx responses are distinguishable for resource-exhausted vs unavailable mapping.
  </behavior>
  <action>Create `internal/client/groq/client_test.go` using `httptest.Server` and deterministic JSON fixtures to cover success, invalid JSON, timeout/cancel, 429, and 5xx paths; ensure returned errors preserve classification needed by handler mapping and update `internal/client/groq/client.go` only as needed for explicit typed/sentinel errors.</action>
  <acceptance_criteria>
    - `rg "TestGroqClientSuccess|TestGroqClientRateLimit|TestGroqClientUnavailable|TestGroqClientTimeout" internal/client/groq/client_test.go` returns matches.
    - `go test ./internal/client/groq -run 'TestGroqClient' -count=1` passes.
    - `go test ./... -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/client/groq -run 'TestGroqClient' -count=1 && go test ./... -count=1</automated>
  </verify>
  <done>Client and full phase regression gate prove deterministic Ask reliability across success and downstream failure modes.</done>
</task>

</tasks>

<verification>
- Targeted tests:
  - `go test ./internal/server/grpc -run 'TestAskSuccess|TestAskStatusMapping' -count=1`
  - `go test ./internal/rag -run 'TestAskPipeline' -count=1`
  - `go test ./internal/client/groq -run 'TestGroqClient' -count=1`
- Full suite:
  - `go test ./... -count=1`
</verification>

<success_criteria>
- RAG-01/02/03 and APC-03 all have deterministic automated checks in place.
- Ask regression risk is controlled by focused package tests plus full-suite phase gate.
</success_criteria>

<output>
After completion, create `.planning/phases/02-rag-ask-delivery/02-rag-ask-delivery-03-SUMMARY.md`
</output>
