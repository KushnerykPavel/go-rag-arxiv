---
phase: 01-contract-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/config.go
  - internal/app/config_test.go
  - internal/app/startup_validation_test.go
  - main/main.go
autonomous: true
requirements:
  - APC-02
must_haves:
  truths:
    - "Operator can boot service without unused required secrets."
    - "Missing runtime-required env values produce explicit and actionable validation messages."
  artifacts:
    - "internal/app/config.go enforces usage-driven required fields and explicit validation."
    - "internal/app/config_test.go verifies required/optional runtime config semantics."
    - "internal/app/startup_validation_test.go verifies startup-path validation error clarity."
  key_links:
    - "main/main.go -> app.Config validation before app.New(cfg, logger).Run(ctx)"
    - "internal/app/config_test.go -> Config.Validate behavior and missing-key message checks"
---

<objective>
Align runtime configuration requirements with real Phase 1 usage and make startup errors operationally clear.

Purpose: Remove required-but-unused config drift and ensure startup validation failures identify actual runtime requirements per APC-02.
Output: Updated config contract, explicit validation entrypoint, and deterministic tests for config/startup behavior.
</objective>

<execution_context>
@/Users/pavelkushneryk/Documents/vsprojects/go-rag-arxiv/.codex/get-shit-done/workflows/execute-plan.md
@/Users/pavelkushneryk/Documents/vsprojects/go-rag-arxiv/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-baseline/01-RESEARCH.md
@internal/app/config.go
@main/main.go
@internal/app/app.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing config behavior tests for usage-driven requirements (APC-02)</name>
  <read_first>
    - .planning/phases/01-contract-baseline/01-RESEARCH.md
    - internal/app/config.go
    - main/main.go
    - internal/app/config_test.go
    - internal/app/startup_validation_test.go
  </read_first>
  <files>internal/app/config_test.go, internal/app/startup_validation_test.go</files>
  <action>Create table-driven tests for `Config.Validate()` and startup validation behavior: (1) passes when `TELEGRAM_TOKEN` + `TELEGRAM_CHAT_ID` set and `GROQ_API_KEY` empty, (2) fails with clear missing-key message when Telegram values absent, and (3) startup path wraps/returns validation error message that names missing runtime-required key(s).</action>
  <acceptance_criteria>
    - `rg "func TestConfigValidation\\(|func TestStartupValidation\\(" internal/app/config_test.go internal/app/startup_validation_test.go` returns matches.
    - `go test ./internal/app -run 'TestConfigValidation|TestStartupValidation' -count=1` fails before Task 2 implementation and passes after.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app -run 'TestConfigValidation|TestStartupValidation' -count=1</automated>
  </verify>
  <done>Tests codify APC-02 behavior and fail on current required-but-unused `GROQ_API_KEY` baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Implement explicit config validation and startup enforcement (APC-02)</name>
  <read_first>
    - internal/app/config.go
    - main/main.go
    - internal/app/config_test.go
    - internal/app/startup_validation_test.go
    - CLAUDE.md
  </read_first>
  <files>internal/app/config.go, main/main.go</files>
  <action>Update `internal/app/config.go` to remove `required:"true"` from `GROQ_API_KEY` and add `func (c Config) Validate() error` that requires only runtime-used fields (`TELEGRAM_TOKEN`, `TELEGRAM_CHAT_ID`) with precise messages (`missing required runtime config: TELEGRAM_TOKEN`, etc.); update `main/main.go` to call `cfg.Validate()` immediately after `envconfig.Process` and fail fast with wrapped validation error.</action>
  <acceptance_criteria>
    - `rg 'GROQ_API_KEY\" required:\"true\"' internal/app/config.go` returns no matches.
    - `rg 'func \\(c Config\\) Validate\\(\\) error' internal/app/config.go` returns one match.
    - `go test ./internal/app -run 'TestConfigValidation|TestStartupValidation' -count=1` passes.
    - `go test ./...` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app -run 'TestConfigValidation|TestStartupValidation' -count=1 && go test ./...</automated>
  </verify>
  <done>Runtime-required env contract matches actual Phase 1 usage and startup errors clearly identify missing required keys.</done>
</task>

</tasks>

<verification>
- Run targeted config/startup checks: `go test ./internal/app -run 'TestConfigValidation|TestStartupValidation' -count=1`
- Run full suite: `go test ./...`
</verification>

<success_criteria>
- APC-02 satisfied: service startup no longer depends on unused required secret (`GROQ_API_KEY`).
- Missing runtime-required values are reported with clear, actionable errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-baseline/01-contract-baseline-02-SUMMARY.md`
</output>
